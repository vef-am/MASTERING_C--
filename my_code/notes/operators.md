# operators.cpp — origins & improvements

- File: `my_code/operators.cpp`

Summary

Course operator examples typically demonstrate syntax and results. This file tightens style and output readability.

What was added or improved (file-specific)

- Removed `using namespace std;` and used `std::` qualifications.
- Enabled `std::boolalpha` so boolean values print as `true`/`false` instead of `1`/`0`.

Why this demonstrates personal work

These are style and output improvements that make the code more professional and easier to read during demonstrations or interviews.
